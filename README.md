# CBS Career fair site

### setup
- Navigate to the folder
- Run `npm install`

### build dev
- Run `npm start` and navigate to [localhost:3000](http://localhost:3000)

### build prod
- Run `npm run build:prod`. Then the "dist" folder will contain the site